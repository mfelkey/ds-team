# WC2026 Soccer Model Architecture

**Run ID:** DS-F233F96C
**Date:** 2026-03-24 12:42 UTC

---

# WC2026 Soccer Model Architecture (ID: DS-F233F96C)

## Executive Summary

The proposed architecture for the WC2026 Dixon-Coles soccer model leverages StatsBomb Open Data via `statsbombpy` and Understat as a gap-fill to create an actionable and accurate prediction system. This plan includes clear steps for data ingestion, feature engineering, model specification, training pipeline design, validation approach, and integration with development team requirements.

## Detailed Evaluation

### 1. DATA INGESTION PLAN
- **Primary Source:** StatsBomb Open Data (WC2018 & WC2022) using `statsbombpy`
- **Data Fields to Extract:**
    - Match IDs
    - Team names and IDs
    - Shot events (xG, outcome, minute)
- **Storage Schema:**
    - SQL Database with tables for matches, teams, shots, and xG data

### 2. FEATURE ENGINEERING
- Compute `home_xg_attack`, `away_xg_defense`, `league_avg_goals` from historical shot-level xG.
- Use these features to align with Dixon-Coles model requirements.

### 3. MODEL SPECIFICATION
- **Implementation Plan:** Estimate parameters for Dixon-Coles adjustment using maximum likelihood estimation (MLE).
- **Recency Weighting:** Apply exponential decay on tournament data to give more weight to recent matches.

### 4. TRAINING PIPELINE
1. Ingest raw StatsBomb data.
2. Compute required features (`xg_attack`, `xg_defense`).
3. Train Dixon-Coles model using MLE.
4. Produce outputs for match predictions and evaluation metrics.

### 5. VALIDATION APPROACH
- **Backtesting:** Validate on WC2018/WC2022 data, focusing on calibration and accuracy of predicted outcomes.
- **Success Criteria:** Model must achieve a minimum threshold of goal prediction accuracy (e.g., within ±0.3 goals) and proper calibration.

### 6. TIMELINE ESTIMATE
- Data Ingestion: 5 days
- Feature Engineering: 7 days
- Model Specification & Training Pipeline: 10 days
- Validation Approach & Testing: 8 days
- Final Adjustments & Reporting: 4 days

Total Estimate: **34 Days**

### 7. DEPENDENCIES
- API Contract for data retrieval and integration.
- Database Access: SQL database setup with necessary tables.

## Recommendation

**GO:** This architecture is feasible within the given timeline and meets all requirements for a reliable Dixon-Coles soccer model. The use of StatsBomb data and Understat gap-fill ensures comprehensive coverage, and the step-by-step approach provides clarity for implementation by the DS team.

### Implementation Notes
- Use `statsbombpy` to extract historical shot-level xG.
- Calculate features such as `xg_attack`, `xg_defense`.
- Implement Dixon-Coles model with MLE.
- Apply exponential decay on tournament data for recency weighting.
- Validate using WC2018/WC2022 datasets.

### Risk Flags
- **Data Coverage:** Ensure comprehensive coverage from StatsBomb and Understat to avoid gaps in the dataset.
- **Model Assumptions:** Validate assumptions of Dixon-Coles model (e.g., homogeneity, independence) on tournament data.

### PRD IMPACT
- No changes required to the current PRD schema.
- Interface contract should include fields for `xg_source_method`, `home_xg_attack`, `away_xg_defense`.

## Detailed Steps

### Data Ingestion Plan
```python
import statsbombpy as sb

# Load WC2018 and WC2022 data
wc2018_matches = sb.matches(competition_id=43, season_id=75)
wc2022_matches = sb.matches(competition_id=43, season_id=99)

# Extract shot-level xG data
def get_shot_data(match_id):
    return sb.events(match_id=match_id, split_by_play_pattern=False)

shot_data_2018 = [get_shot_data(m['match_id']) for m in wc2018_matches]
shot_data_2022 = [get_shot_data(m['match_id']) for m in wc2022_matches]

# Save to database
def save_to_db(data, table_name):
    # Assume a function that saves dataframes to specified SQL tables

save_to_db(wc2018_matches, 'wc2018_matches')
save_to_db(wc2022_matches, 'wc2022_matches')

for match_id in wc2018_matches['match_id']:
    save_to_db(get_shot_data(match_id), f'shots_{match_id}')

for match_id in wc2022_matches['match_id']:
    save_to_db(get_shot_data(match_id), f'shots_{match_id}')
```

### Feature Engineering
```python
def compute_features(shots, teams):
    # Example feature computation logic for xg_attack and xg_defense
    pass

features_2018 = [compute_features(get_shot_data(m['match_id']), wc2018_matches) for m in wc2018_matches]
features_2022 = [compute_features(get_shot_data(m['match_id']), wc2022_matches) for m in wc2022_matches]

save_to_db(features_2018, 'features_wc2018')
save_to_db(features_2022, 'features_wc2022')
```

### Model Specification
```python
from scipy.optimize import minimize

# Dixon-Coles model implementation
def dixon_coles_likelihood(params, home_goals, away_goals):
    # Define likelihood function for MLE
    pass

initial_params = [1.0] * 5  # Example initial parameters
result = minimize(dixon_coles_likelihood, initial_params, method='BFGS')

# Extract and save model parameters
model_params = result.x
```

### Training Pipeline
```python
from sklearn.model_selection import train_test_split

# Load features from database
features_2018_df = load_from_db('features_wc2018')
features_2022_df = load_from_db('features_wc2022')

X_train, X_val, y_train, y_val = train_test_split(features_2018_df + features_2022_df)

# Train model
model_params = dixon_coles_likelihood(X_train, y_train)
```

### Validation Approach
```python
def backtest_model(model_params, validation_data):
    # Example function for backtesting the Dixon-Coles model
    pass

validation_results = backtest_model(model_params, X_val)

# Evaluate results against success criteria
```

This structured approach ensures a clear path from raw data to trained and validated models, adhering to real-world implementation requirements.